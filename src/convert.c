#include "convert.h"

vec set_to_vec (set this) {
	vec new;
	this.val = new.val;
	this.size = new.size;
	return new;
}

set vec_to_set (vec this) {
	set new;
	this.val = new.val;
	this.size = new.size;
	return new;
}
